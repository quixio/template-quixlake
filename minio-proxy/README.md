# MinIO Proxy

This allows the use of both the UI and API endpoints
of MinIO when accessing MinIO from an external project.

You can connect to the api by hitting the `/api` endpoint, 
else the root endpoint (`/`) goes to the UI.
